<%= form_for(hi) do |f| %>
  <% if hi.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(hi.errors.count, "error") %> prohibited this hi from being saved:</h2>

      <ul>
      <% hi.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :count %>
    <%= f.text_field :count %>
  </div>

  <div class="field">
    <%= f.label :desc %>
    <%= f.text_field :desc %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
